#include <stdio.h>

/**
 * main - Entry point
 * Description: Alphabet in reverse
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int i;

	for (i = 'z'; 1 >= 'a'; i--)
	{
		putchar(i);
	}

	putchar('\n');

	return (0);
}
