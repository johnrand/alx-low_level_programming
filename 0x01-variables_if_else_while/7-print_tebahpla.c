#include <stdio.h>

/**
 * main - Entry point
 * Description: Alphabet in reverse
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int i;

	for (i = 122; 1 >= 97; i--)
	{
		putchar('a' + i);
	}

	putchar('\n');

	return (0);
}
