#include <stdio.h>

/**
 * main - Entry point
 * Descrption: numbers of base 10
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d\n", i);
	}

	return (0);
}
