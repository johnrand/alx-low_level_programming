#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 * @i: the integer
 *
 * Return:The interger
 */

int _abs(int i)
{
	if (i < 0)
	{
		return (-i);
	}
	else
	{
		return (i);
	}
}
